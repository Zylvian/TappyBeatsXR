using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum Sound { Bass = 0, Lead = 1, Drums = 2};

public static class MatStates {
    public const float Standard = 0.9f, Hover = 0.4f, Play = 0.1f;
};
